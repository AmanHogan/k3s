spring.application.name=commitment-tracker
# MONGODB_URI env var overrides for k8s; falls back to local default for dev
spring.data.mongodb.uri=${MONGODB_URI:mongodb://localhost:27017/commitment_tracker}
#logging.level.org.springframework.data.mongodb=DEBUG
#logging.level.org.mongodb.driver=DEBUG